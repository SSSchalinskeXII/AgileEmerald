<!DOCTYPE html>
<html lang="en-US">
<head>
	<title>Musk's Journey to Mars</title>
	<script src="javascripts/phaser.js"></script>
	<script src="javascripts/jquery-3.3.1.min.js"></script>
	<%= stylesheet_link_tag 'style', media: 'all', 'data-turbolinks-track' => true %>
	<%= csrf_meta_tags %>
</head>
<body>
	<div id="gameContainer">
		<header>
			<div id="title"></div>
		</header>
		      
		<div class="container">
                           <% if current_user %>
            <div class="userInfo">
 <span id="email"><%= current_user.email %></span>
				    <%= image_tag current_user.image, class: "img-circle", width: 24 %> 
				    Signed in as <strong><%= current_user.name %></strong>!
				    <%= link_to "Sign out", signout_path, id: "sign_out" %>
            </div>
			<% end %>
			<%= yield %>
		</div>
		<% if current_user %>
			<section class="content"></section>
			<%= yield %>
		<% else %>
			<%= link_to "Sign in with Google", "/auth/google_oauth2", id: "sign_in" %>
		<% end %>
		
		<footer>
			<h4>Copyright <%=Date.today.year %> Musk's Journey to Mars</h4>
		</footer>
	</div>
</body>
<% if current_user %>
	<script src="javascripts/game.js" data-turbolinks-track="false"></script>
<% end %>
</html>
